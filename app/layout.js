export const metadata = {
title: "PPF Manager",
description: "PPF scheduling app",
};

export default function RootLayout({ children }) {
return (
<html lang="en">
<body>{children}</body>
</html>
);
}
